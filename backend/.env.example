# Copy to .env and fill in from the Supabase dashboard:
#   Project Settings → Database → Connection string
#
# .env is gitignored. Never commit real credentials.

# THE APP'S CONNECTION — transaction pooler, port 6543.
# Short-lived connections need a pooler. Supavisor's transaction mode has no
# prepared statements, which is why app/db.py sets statement_cache_size=0 and
# uses NullPool.
DATABASE_URL=postgresql://postgres.your-project-ref:PASSWORD@aws-0-region.pooler.supabase.com:6543/postgres

# ALEMBIC'S CONNECTION — session pooler, port 5432.
# DDL takes locks and needs one serial conversation with the database. Running
# migrations through the transaction pooler above is unreliable.
#
# THE PORT IS THE ONLY DIFFERENCE between these two lines, and getting it wrong
# produces intermittent failures rather than an obvious error. Check it twice.
DIRECT_URL=postgresql://postgres.your-project-ref:PASSWORD@aws-0-region.pooler.supabase.com:5432/postgres

# If your database password contains @ / : or #, URL-encode it, or the URL
# parses wrong and the error will not tell you that is why.
