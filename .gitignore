# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependencies. Unanchored on purpose: these live inside each workspace folder
# (frontend/node_modules, backend/.venv), not at the repo root, so a leading
# slash would match nothing.
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Python virtualenvs and bytecode. Same reasoning as node_modules above: these
# live inside backend/, not at the repo root, so no leading slash.
.venv/
venv/
__pycache__/
*.py[cod]
.ruff_cache/
.mypy_cache/
.pytest_cache/

# testing
coverage/

# next.js
.next/
out/

# production
build/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...but the templates must be committed, or a fresh clone has nothing to copy.
# frontend/.env.example predates the rule above and survives only because it is
# already tracked; a new one would be silently ignored without this line.
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# claude skill reports
claudeskill_reports/

# Agent-generated design specs and implementation plans: local scratch, not
# project documentation. frontend/docs/backend-integration.md and
# frontend/docs/shadcn.md are the real docs.
docs/superpowers/
