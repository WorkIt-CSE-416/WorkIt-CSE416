# Base URL of the Python API (backend/). Read only in Server Actions — see
# src/lib/auth.ts — so this is deliberately not NEXT_PUBLIC_*: nothing here
# needs to reach client JS.
API_URL=http://localhost:8000
